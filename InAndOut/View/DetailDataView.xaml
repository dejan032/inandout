<UserControl x:Class="InAndOut.View.DetailDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InAndOut.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding DetailData, Source={StaticResource Locator}}">
    <Grid>
        <ListView Margin="10" Name="entries" ItemsSource="{Binding RawData}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Id" Width="120" DisplayMemberBinding="{Binding Id}" />
                    <GridViewColumn Header="UserId" Width="120" DisplayMemberBinding="{Binding UserId}" />
                    <GridViewColumn Header="In Time" Width="50" DisplayMemberBinding="{Binding InTime}" />
                    <GridViewColumn Header="Out Time" Width="150" DisplayMemberBinding="{Binding OutTime}" />
                    <GridViewColumn Header="Create Time" Width="150" DisplayMemberBinding="{Binding TimeStamp}" />
                    <GridViewColumn Header="Duration" Width="150" DisplayMemberBinding="{Binding Duration}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
