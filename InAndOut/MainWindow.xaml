<Controls:MetroWindow x:Class="InAndOut.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:ignore="http://www.ignore.com"
        mc:Ignorable="d ignore"
        d:Height="300"
        d:Width="300"
        Title="InAndOut"
        SizeToContent="WidthAndHeight"
        DataContext="{Binding Main, Source={StaticResource Locator}}" Height="498" Width="637">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Skins/MainSkin.xaml" />
                <ResourceDictionary Source="Resources/Icons.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid x:Name="LayoutRoot">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <ToggleButton 
            Grid.Column="3" Grid.Row="0"
            Width="50"
              Height="50"
              Margin="0, 10, 0, 0"
              Style="{DynamicResource MetroCircleToggleButtonStyle}">
            <Rectangle Width="20"
               Height="20"
               Fill="{DynamicResource BlackBrush}">
                <Rectangle.OpacityMask>
                    <VisualBrush Stretch="Fill"
                         Visual="{DynamicResource appbar_settings}"/>
                </Rectangle.OpacityMask>
            </Rectangle>
        </ToggleButton>

        <Label Content="Working:" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Center" Margin="25" VerticalAlignment="Center" FontFamily="Sergio UI" FontSize="20" FontWeight="Medium"
               Visibility="{Binding WorkTimerVisibility,FallbackValue=Visible}"/>
        <TextBlock Text="{Binding WorkTimePassedText,FallbackValue=0:00:00}" Grid.Column="2" Grid.Row="1" HorizontalAlignment="Center" Margin="25" VerticalAlignment="Center" FontFamily="Sergio UI" FontSize="20" FontWeight="Bold"
                   Visibility="{Binding WorkTimerVisibility,FallbackValue=Visible}"/>
        <Label Content="Break:" Grid.Column="1" Grid.Row="2" HorizontalAlignment="Center" Margin="25" VerticalAlignment="Center" FontFamily="Sergio UI" FontSize="20" FontWeight="Medium"
               Visibility="{Binding BreakTimerVisibility,FallbackValue=Collapsed}"/>
        <TextBlock Text="{Binding BreakTimePassedText,FallbackValue=0:00:00}" Grid.Column="2" Grid.Row="2" HorizontalAlignment="Center" Margin="25" VerticalAlignment="Center" FontFamily="Sergio UI" FontSize="20" FontWeight="Bold"
                    Visibility="{Binding BreakTimerVisibility,FallbackValue=Collapsed}"/>
        <Button Content="{Binding PunchClockButtonText, FallbackValue='Punch In'}" Grid.Column="1" Grid.Row="3" Style="{DynamicResource SquareButtonStyle}"  Height="50" Width="80" Margin="10" Command="{Binding PunchClockButtonCommand}"/>
        <Button Content="{Binding BreakButtonText, FallbackValue='Start Break'}" Grid.Column="2" Grid.Row="3" Style="{DynamicResource SquareButtonStyle}" Height="50" Width="80" Margin="10" Command="{Binding BreakClockButtonCommand}" IsEnabled="{Binding BreakButtonIsEnabled,FallbackValue=False}"/>

    </Grid>
</Controls:MetroWindow>
